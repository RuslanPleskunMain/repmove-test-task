# Base URL override (optional)
# REPMOVE_BASE_URL=https://dev-repmove-enterprise.web.app

# Credentials for an existing staging user (required for login flows)
# Run `npm run setup:test-user` to auto-generate disposable creds
TEST_USER_EMAIL=
TEST_USER_PASSWORD=
